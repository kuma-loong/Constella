# 0.1.5 发布审核记录

状态：候选包已准备，等待维护者审核；尚未推送、创建标签、上传 PyPI 或部署。

## 合并与内容

浏览器恢复与历史刷新修复 `ea22947` 已 fast-forward 合并到本地 `main`。版本准备位于 `codex/release-0.1.5`。
五个 Python 发行包、前端、锁文件及安装文档的版本统一为 0.1.5。
完整版本说明见 [CHANGELOG](../../CHANGELOG.md)。

本次版本包括个人活动面板，以及浏览器休眠恢复、手动刷新历史曲线和热力图、请求超时与错误重试、数据库读写隔离等修复。
构建脚本改为在临时源码副本中构建，避免覆盖源码部署正在使用的前端资源。

## 候选包与验证

本地 `dist/0.1.5/` 包含 backend、web、lab、tui、full 五种发行版的 5 个 wheel、5 个源码包和 `SHA256SUMS`。
候选包生成后，0.1.5 更新日志已补充中文翻译；当前候选源码包仍含原英文日志，正式发布前须重新构建并更新校验和。此次仅修改文档和项目规范，运行代码未变。
生成文件不提交 Git；构建方式见 [PACKAGING](../PACKAGING.md)。

- 后端及包测试：229 passed；前端回归：9 passed。
- Ruff 和 `git diff --check` 通过。
- Core、Lab 两种前端在隔离目录内构建通过。
- Twine 7.0.0 对全部 10 个包的元数据校验通过。
- 五个源码包分别重建 wheel，成员路径与文件内容均与候选 wheel 一致。
- 五种发行版分别安装到独立环境，版本、CLI、模块归属及适用的 HTTP 健康检查、静态页面检查通过。
- 修复阶段已在隔离预览中检查桌面明暗主题及 390px 手机视口，并验证冻结恢复、网络恢复与历史请求失败后的重试。尚未在生产 Mac 浏览器上进行实际合盖验收。

## 隐私检查

检查覆盖当前 Git 跟踪源码、全部候选包、各引用可达的 1,159 个历史 Git blob，以及文档演示图片。
密钥扫描关闭网络验证，未向外部服务提交候选密钥。

- 当前源码与发布包未检出真实密钥；当前生产令牌和身份配置字面量比对无命中。
- 发布包未包含运行数据库、日志、环境配置、令牌文件或本机用户名。
- 历史通用扫描的 2,603 个高熵命中全部核实为 npm/Cargo 依赖校验和，无未解释命中。
- 其他旧分支的 19 个历史 blob 含本机用户名路径，涉及旧 README、运维文档及 Rust 重写计划；这些 blob 不在本次发布提交的祖先链中。未重写其他分支历史，不能将本报告理解为所有旧分支都已脱敏。
- 8 张文档 PNG 人工检查显示演示节点与用户，未发现私人内容；无 PNG 元数据或 EXIF。两个 GIF 分别抽查首、中、尾帧，均为演示内容；未逐帧审查。
- 新增环境文件、数据库及 PyPI 凭据文件的 Git 忽略规则，降低后续误提交风险。

以上是已执行范围内的扫描与人工复核结果，不是对未知秘密或全部图像帧的绝对保证。
详细报告仅保留在本地 `run/preview-release-0.1.5/`，不进入发行包。

## 清理与生产状态

清理闲置预览浏览器缓存、废弃安装环境和测试缓存，已清理的旧文件分配空间合计约 672 MiB；这不等于并发写入期间的磁盘可用空间增量。
本次验证产生的临时环境与解包副本也在验证结束后清理，保留构建包、校验和、日志和审计结果。

保留生产环境、运行数据库、令牌配置、当前使用中的预览、用户文件和旧版本发布包。
生产 8765/8766 进程及数据库路径未改变，未重启或部署。最终健康检查为健康、2 个在线节点、写入队列为空。
历史磁盘满错误计数仍为 1,519，当前连续错误为 0；目录清理不能替代对生产数据盘空间的持续管理。

## 审核后才能进行

维护者确认版本内容与候选包后，再确定正式发布日期、合并版本准备提交、创建版本标签和执行正式上传。
若审核产生代码或包内文档变更，应重新构建并校验候选包。当前未执行任何发布操作。
